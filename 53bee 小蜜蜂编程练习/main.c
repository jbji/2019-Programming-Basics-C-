#include <stdio.h>

typedef struct{
    int digits[500];
}num;

int main() {
    void stor(int,num);
    void add(num,num);
    num fabo(num);

    int a,b;
    scanf("%d %d",a,b);

    printf("Hello, World!\n");
    return 0;
}

num fabo(num in){
    num

}
